name: linkedin_job_matcher
description: Flutter Android app for LinkedIn-style job matching and alerts.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  firebase_core: ^3.2.0
  firebase_messaging: ^15.0.2
  flutter_local_notifications: ^17.2.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
