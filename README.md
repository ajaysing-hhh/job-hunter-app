# Job Hunter App

Monorepo containing:

- `android_app_flutter/` – Flutter Android app for job alerts and match results
- `backend_scanner/` – Python backend that scans jobs and triggers notifications
- `resume_matching_system/` – AI-based resume vs job description matcher module

See each folder README for setup.
